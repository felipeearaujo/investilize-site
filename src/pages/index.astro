---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import FormattedDate from '../components/FormattedDate.astro';
import DashboardIndicadores from '../components/DashboardIndicadores.astro';
import { getCollection } from 'astro:content';

const pageTitle = "Investilize | Seu Portal Financeiro e Administrativo";
const pageDescription = "Ferramentas financeiras, calculadoras de IR e artigos técnicos para ajudar você a tomar decisões inteligentes com seu dinheiro.";

const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
).slice(0, 3);
---

<!doctype html>
<html lang="pt-BR">
<head>
    <BaseHead title={pageTitle} description={pageDescription} />
    </head>
<body>
    <Header />
    <main>
        <section class="hero-section">
            <div class="hero-content">
                <h1 class="hero-title">Educação Financeira com Autoridade</h1>
                <p class="hero-subtitle">Dados oficiais do Banco Central e ferramentas precisas para sua gestão pessoal e profissional.</p>
                <div class="hero-actions">
                    <a href="/ferramentas/juros-compostos/" class="hero-button">Calcular Investimentos</a>
                    <a href="/ferramentas/calculadora-ir-mensal/" class="hero-button secondary">Simular IR 2026</a>
                </div>
            </div>
        </section>

        <div class="dashboard-wrapper" style="max-width: 900px; margin: -1.5rem auto 2rem; padding: 0 1rem;">
            <DashboardIndicadores />
        </div>

        <section class="features-section">
            <h2 class="section-title">Ferramentas de Precisão</h2>
            <div class="features-grid">
                <a href="/ferramentas/calculadora-ir-mensal/" class="feature-card">
                    <div class="icon-wrapper">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M2.25 18.75a60.07 60.07 0 0 1 15.797 2.101c.727.198 1.453-.342 1.453-1.096V18.75M3.75 4.5v.75A.75.75 0 0 1 3 6h-.75m0 0v-.375c0-.621.504-1.125 1.125-1.125H20.25M2.25 6v9m18-10.5v.75c0 .414.336.75.75.75h.75m-1.5-1.5h.375c.621 0 1.125.504 1.125 1.125v9.75c0 .621-.504 1.125-1.125 1.125h-.375m1.5-1.5H21a.75.75 0 0 0-.75.75v.75m0 0H3.75m0 0h-.375a1.125 1.125 0 0 1-1.125-1.125V15m1.5 1.5v-.75A.75.75 0 0 0 3 15h-.75M15 10.5a3 3 0 1 1-6 0 3 3 0 0 1 6 0Zm3 0h.008v.008H18V10.5Zm-12 0h.008v.008H6V10.5Z" /></svg>
                    </div>
                    <h3>Imposto de Renda</h3>
                    <p>Calcule seu IR mensal (Carnê-Leão) e veja o impacto da Selic em caso de atraso.</p>
                </a>

                <a href="/ferramentas/juros-compostos/" class="feature-card">
                    <div class="icon-wrapper">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M15.75 15.75V18m-7.5-6.75h.008v.008H8.25v-.008Zm0 3h.008v.008H8.25v-.008Zm0 3h.008v.008H8.25v-.008Zm3.75-3h.008v.008h-.008v-.008Zm0 3h.008v.008h-.008v-.008Zm0-6h.008v.008h-.008v-.008Zm3-3h.008v.008h-.008v-.008Zm-9-3h.008v.008H8.25v-.008Zm3 0h.008v.008h-.008v-.008ZM21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" /></svg>
                    </div>
                    <h3>Juros Compostos</h3>
                    <p>Projete o crescimento do seu patrimônio com aportes mensais e taxas customizadas.</p>
                </a>

                <a href="/ferramentas/conversor-de-moedas/" class="feature-card">
                    <div class="icon-wrapper">
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" d="M12 6v12m-3-2.818.879.659c1.171.879 3.07.879 4.242 0 1.172-.879 1.172-2.303 0-3.182C13.536 12.219 12.768 12 12 12c-.725 0-1.45-.22-2.003-.659-1.106-.826-2.25-2.226-2.25-4.154 0-1.928.984-3.597 2.5-4.596" /></svg>
                    </div>
                    <h3>Conversor de Moedas</h3>
                    <p>Cotações em tempo real para planejar suas viagens ou investimentos internacionais.</p>
                </a>
            </div>
        </section>

        <section class="posts-section">
            <h2 class="section-title">Últimos Artigos</h2>
            <div class="posts-wrapper">
                <ul class="posts-grid">
                    {posts.map(post => (
                        <li class="post-card">
                            <a href={`/blog/${post.id}/`}>
                                <h3 class="post-title">{post.data.title}</h3>
                                <p class="post-date"><FormattedDate date={post.data.pubDate} /></p>
                                <p class="post-description">{post.data.description}</p>
                                <span class="read-more">Ler Artigo Completo &rarr;</span>
                            </a>
                        </li>
                    ))}
                </ul>
                <div class="all-posts-link">
                    <a href="/blog/">Acessar o Blog Completo</a>
                </div>
            </div>
        </section>
    </main>
    <Footer />

    <style>
        /* Variáveis de Cor - Padronização Roxo */
        :root {
            --primary: #7c3aed;
            --primary-hover: #6d28d9;
            --primary-light: #f5f3ff;
            --text-main: #111827;
            --text-muted: #4b5563;
            --bg-light: #f9fafb;
        }

        /* Hero Section */
        .hero-section {
            text-align: center;
            padding: 5rem 1.5rem;
            background-color: var(--bg-light);
            border-bottom: 1px solid #e5e7eb;
        }
        .hero-content { max-width: 800px; margin: 0 auto; }
        .hero-title { font-size: 3.5rem; font-weight: 800; color: var(--text-main); line-height: 1.1; margin-bottom: 1.5rem; }
        .hero-subtitle { font-size: 1.25rem; color: var(--text-muted); margin-bottom: 2rem; }
        .hero-actions { display: flex; gap: 1rem; justify-content: center; }
        
        .hero-button {
            padding: 0.8rem 2rem;
            background-color: var(--primary);
            color: white;
            font-weight: 600;
            border-radius: 10px;
            text-decoration: none;
            transition: 0.3s;
        }
        .hero-button:hover { background-color: var(--primary-hover); transform: translateY(-2px); }
        .hero-button.secondary { background-color: white; color: var(--primary); border: 2px solid var(--primary); }
        .hero-button.secondary:hover { background-color: var(--primary-light); }

        /* Dashboard Wrapper */
        .dashboard-wrapper { max-width: 1000px; margin: -2rem auto 3rem; padding: 0 1.5rem; position: relative; z-index: 10; }

        /* Features Section */
        .features-section { padding: 4rem 1.5rem; }
        .section-title { text-align: center; font-size: 2.2rem; font-weight: 700; margin-bottom: 3rem; color: var(--text-main); }
        .features-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; max-width: 1100px; margin: 0 auto; }
        
        .feature-card {
            background: white;
            padding: 2.5rem;
            border-radius: 16px;
            border: 1px solid #e5e7eb;
            text-decoration: none;
            color: inherit;
            transition: 0.3s ease;
        }
        .feature-card:hover { border-color: var(--primary); box-shadow: 0 20px 25px -5px rgba(124, 58, 237, 0.1); transform: translateY(-5px); }
        
        .icon-wrapper {
            background-color: var(--primary-light);
            padding: 0.8rem;
            border-radius: 12px;
            display: inline-block;
            margin-bottom: 1.5rem;
            color: var(--primary);
        }
        .icon-wrapper svg { width: 2.5rem; height: 2.5rem; }
        .feature-card h3 { font-size: 1.4rem; margin-bottom: 0.8rem; color: var(--text-main); }
        .feature-card p { color: var(--text-muted); line-height: 1.6; }

        /* Posts Section */
        .posts-section { padding: 5rem 1.5rem; background-color: var(--bg-light); }
        .posts-wrapper { max-width: 1100px; margin: 0 auto; }
        .posts-grid { list-style: none; padding: 0; display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 2rem; }
        
        .post-card {
            background: white;
            border-radius: 16px;
            box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05);
            transition: 0.3s;
        }
        .post-card:hover { transform: scale(1.02); box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1); }
        .post-card a { display: block; padding: 2rem; text-decoration: none; color: inherit; }
        .post-title { font-size: 1.3rem; color: var(--text-main); margin-bottom: 0.5rem; transition: 0.2s; }
        .post-card:hover .post-title { color: var(--primary); }
        .post-date { color: var(--text-muted); font-size: 0.85rem; margin-bottom: 1rem; }
        .post-description { font-size: 0.95rem; color: var(--text-muted); margin-bottom: 1.5rem; line-height: 1.5; }
        .read-more { font-weight: 700; color: var(--primary); font-size: 0.9rem; }

        .all-posts-link { text-align: center; margin-top: 4rem; }
        .all-posts-link a { font-weight: 700; color: var(--primary); text-decoration: none; font-size: 1.2rem; border-bottom: 2px solid transparent; transition: 0.2s; }
        .all-posts-link a:hover { border-bottom-color: var(--primary); }

        /* Responsividade */
        @media (max-width: 768px) {
            .hero-title { font-size: 2.5rem; }
            .hero-actions { flex-direction: column; }
            .hero-button { width: 100%; }
        }
    </style>
</body>
</html>